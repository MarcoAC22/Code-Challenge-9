const mongoose = require("mongoose")

var bannedWords = mongoose.Schema({
    data: [
        {
            bum
            dogs
            kardashain
            poo
            rude
            seneca
        },
    ],

module.exports = mongoose.model("bannedWords", bannedSchema);